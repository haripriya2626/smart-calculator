# Smart Calculator

React + Vite deployment-ready version of the Claude artifact.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The app is client-side only; no backend is required for the current version.
